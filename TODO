* map sensitive data to user defined variables ?
* handle check mode ?
* handling of Secrets ?
* implements new AWS modules
* implements GCP modules
* implements Azure modules
* some rollback actions use the "present" value and are so considered to be rollbacked !
